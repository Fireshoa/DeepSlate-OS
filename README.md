# DeepSlate-OS
A graphical operating system build for ComputerCraft: Tweaked that uses Basalt for its GUI.
* I have not uploaded the OS to this repo yet. This repo will be used to store the OS in the future, so only a `wget run` command has to be run.
